# Application Configuration
NODE_ENV=production
PORT=3001

# JWT Configuration (REQUIRED - Generate a secure random string)
# Generate with: openssl rand -base64 64
JWT_SECRET=your-super-secure-jwt-secret-change-this-in-production

# Database (SQLite - path is relative to app root, persisted via Docker volume)
# DB_PATH=/app/server/database/bnbinsights.db

# CORS Configuration (optional - comma-separated origins)
# CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com

# Rate Limiting (optional)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
